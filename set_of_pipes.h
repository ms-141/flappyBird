#ifndef SET_OF_PIPES_H
#define SET_OF_PIPES_H

typedef struct
{
    unsigned int x, y; // each set of pipes has a fixed vertical pos.
                       // but a continuously changing horizontal pos.
} SetOfPipes;

#endif